angular.module('directives.searchResultList', []);
angular.module('directives.searchResultList').directive('searchResultList', function() {
  return {
    this.title=title;
    this.issue=issue;
    this.files=files;
  };
});
